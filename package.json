{
  "name": "kazer",
  "private": true,
  "version": "1.0.0",
  "description": "Kazer static interface with a secure Groq chat function",
  "scripts": {
    "security:check": "node scripts/security-check.mjs",
    "integration:test": "node scripts/mcp-smoke.mjs && node scripts/api-smoke.mjs",
    "security:audit": "npm audit --omit=dev"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mammoth": "^1.8.0",
    "pdf-parse": "^2.4.5"
  }
}
