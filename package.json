{
  "name": "kazer",
  "private": true,
  "version": "1.0.0",
  "description": "Kazer static interface with a secure Groq chat function",
  "scripts": {
    "security:check": "node scripts/security-check.mjs",
    "chat:regression": "node scripts/chat-regression.mjs",
    "check": "npm run security:check && npm run chat:regression",
    "integration:test": "node scripts/mcp-smoke.mjs && node scripts/api-smoke.mjs && node scripts/github-oauth-smoke.mjs && node scripts/production-config-smoke.mjs",
    "security:audit": "npm audit --omit=dev"
  },
  "engines": {
    "node": ">=20"
  }
}
